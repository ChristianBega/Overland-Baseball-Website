1. when i click on the tab to toggle between different seasons
2. the tab value it self seems to update moving from 1 season to the next as expected
3. the corresponding tab fields however, they do you seem to be rendering in the corresponding input fields for that tab. Ex - I click the spring tab, I see the spring input fields. I click the summer tab, I still see the spring input fields. The summer input fields never render in, despite the tab state clearly being updated.... 


Important note - 
1. I noticed the selectedSeason value is being updated, hence the tabs properly updating...
2. The selectedSeason however that is passed to the cmsSeasonTabsOptions remains as "spring"... and does not update despite the selectedSeason actually changing... 


Is the issue because we run the shouldShowField as a initial render??? And maybe we aren't rerunning it???? Or will it run on rerender?