Tried to add responsive logic, on desktop the edit button renders the scheduleContentEditable, on mobile it renders the a modal using our useModal hook. 


The issue is the cmsEdit.context does not work outside of the admin dashboard, the modal provider lies outside of the admin dashboard, so when we pass the cmsEdit content to the Modal, it does not access the context properly....


Another issue, because of the nature of the modal, I'm unsure if we will be able to use the cmsEdit.context to update the content of the modal.



Attempt #1 - Tried to pass the cmsEdit.context from a parent component (actionButtonCell) to the modal component, but it was not working. I think the issue is with how the props are initial passed to the modal component. Because they only render when a edit button is clicked, so it will capture the initial state values, however when the user begins to edit, then the modal component will not update the state values and we would need to rely on the cmsEdit.context to update the state values. But we cannot pass the cmsEdit.context to the modal component because it lies outside of the admin dashboard.

Attempt #2 - Tried to adjust the modal providers to allow the modal to have access to the cmsEdit.context, but it was not working. And despite moving the modal provider to the admin dashboard, it still did not work.

Ideas for other solutions: 
1. Create a local state to replace the cmsEdit.context?
2. redux??? instead of context might give us more control?
3. we have a cms provider context that is a parent to all other contexts. What if we keep the cms edit context, but just set a new state value in the parent cmsProvider context? If we tracked  any changes to the fields, we could access that value from our modal component and just leverage that value instead of the cmsEdit.context? it would reflect the same value, just be in the parent context and accessable to the modal component.





















WHEN a user clicks on the edit button : 
1. open modal
2. render form and prefill with the current item data 





1. move modal provider to admin dashboard under the cmsEdit.context
2. prop drill from the action button cell : 
   a. currentEditableItemData - prefill the form with this data
   b. cmsOperationStatus - to update the status of the operation
3. instead of using the cmsEdit.context, we will use the react-hook-form to capture the data, and then we will just make the request from the handleSubmit function on the form.



The idea behind this would to be reduce the amount of code we need to rely on. Instead of relying on the cmsEdit.context to update the state values, we will just use the react-hook-form to capture the data, and then we will just make the request from the handleSubmit function on the form. By doing this we reduce to a "single source of truth" for the data (react-hook-form).


